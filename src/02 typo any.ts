// Utilize any apenas no ultimo caso , ele resebe qualquer coisas
function showMessage (msg: any){
    return msg;
}

// Qual quer coisa

showMessage ('oi');
showMessage ([1, 1, 1 ]);
showMessage (true);
